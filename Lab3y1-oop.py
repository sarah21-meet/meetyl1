class Animal(object):
	def __init__(self,sound,name,age,favorite_color):
		self.sound = sound
		self.name = name
		self.age = age
		self.favorite_color = favorite_color
	def eat(self,food):
		print('Yummy!!' + self.name + 'is eating' + food)
	def description(self):
		print(self.name +'is' + self.age +'years old and loves the color'+ self.favorite_color)
	def make_sound(self,sound):
		for i in range():
		print(sound)
	dog = Animal('bark','smokey','5','indigo')	
	dog.discription()
	dog.eat('Spaghetti')

class Person(object):
	def __init__(self,name,age,city,gender):
		self.name = name
		self.age = age
		self.city = city
		self.gender = gender
	def description(self):
		print(self.name + 'is' + self.age, + 'lives in'+self.city +'and their gender is'+self.gender)

	def eat(self,food):
		print(self.name+'is eating'+food)
	def Nationality(self,Nationality):
		print(self.name+'is' +Nationality):
Persy = Person('')